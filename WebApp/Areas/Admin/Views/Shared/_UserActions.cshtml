@if (ViewBag.ShowActions == true)
{
    @Html.ActionLink("Documents", "DocumentList", new { id = ViewBag.id })<span> | </span>
    @Html.ActionLink("Edit", "Edit", new { id = ViewBag.id })<span> | </span>
    @Html.ActionLink("Details", "Details", new { id = ViewBag.id })
    if (ViewBag.DeleteActions == true)
    {
        <span> | </span>
        @Html.ActionLink("Delete", "Delete", new { id = ViewBag.id, UserId = ViewBag.UserId, file = ViewBag.file }, new { onclick = "return confirm('Do you want to delete this employee\\'s record?')" })
    }
}